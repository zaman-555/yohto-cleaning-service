# Browser-visible API URL (login/register from the client)
NEXT_PUBLIC_API_BASE_URL=https://yohto-cleaning-service.vercel.app

# Optional: private URL for Server Components / Server Actions (Docker, etc.)
# API_BASE_URL=http://server:5000
